[project]
name = "abcdigits"
version = "0.1.0"
description = "ABCDigits: a semantics-free synthetic key-value retrieval benchmark for language models"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "MIT" }
dependencies = [
    "transformers>=4.18.0",
]
